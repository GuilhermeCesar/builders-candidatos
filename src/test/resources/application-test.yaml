test:
  api:
    v1:
      basePath: "/api/v1/clientes"


spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS cliente\;SET SCHEMA cliente'
    username: sa
    password: password